
set axiom = "a"
set table_func(i, j) = "t1"  # Invalid

# a => FFF        # Valid
a -> aFa        # Valid
a -> aBa        # Valid
rules {
    F -> avd    # Valid
    v =>        # Valid
}
coding rules {
    r -> rr     # Invalid
    r => k      # Valid
}
production rules {
    k => F-F    # Invalid
    k<_ -> F-F  # Valid
}
table t1 {
	a -> b      # Valid
	b => c      # Invalid
	e           # Invalid
}
u => [+F]       # Valid
